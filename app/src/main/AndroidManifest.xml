<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="one.jamaa.appjamaa">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".welcome.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".registration.RegistrationActivity" />
        <activity android:name=".registration.LoginActivity" />
        <activity android:name=".registration.ResetActivity" />
        <activity android:name=".welcome.WelcomeActivity" />
        <activity android:name=".main.profile.ProfileListActivity" />
        <activity android:name=".main.wallet.WalletActivity" />
        <activity android:name=".main.exchange.ExchangeActivity" />
        <activity android:name=".main.contract.ContractActivity" />
        <activity android:name=".main.home.HomeActivity" />
        <activity android:name=".main.contract.fragment.NewContractFragment3" />

    </application>


</manifest>